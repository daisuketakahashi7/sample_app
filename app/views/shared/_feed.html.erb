<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <!--コントローラとアクションを明示的に設定-->
  <%= will_paginate @feed_items,
                    params: { controller: :static_pages, action: :home } %>
<% end %>